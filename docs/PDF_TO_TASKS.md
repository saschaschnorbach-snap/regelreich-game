# PDF to Tasks

## Must
- Preserve 5-part progression plus avatar intro scene (0..5 mapping in runtime).
- Keep host selection and persistent host identity.
- Encode fixed chapter structure in data (`step.type`).
- Support retry-with-feedback loops (no punitive dead ends).
- Maintain three strategy tracks: Empörung, Konsens, Diffamierung.

## Should
- Expand activity fidelity to match script formats (e.g., highlight/drag-drop placeholders).
- Add admin templates per step type to reduce authoring errors.
- Add QA checks for strategy keyword presence per scene.
